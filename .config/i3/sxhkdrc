# vim: set foldmethod=marker foldlevel=0 nomodeline:
# i3-specific sxhkd config, extends ~/.config/sxhkd/sxhkdrc

# close and kill
super + {_,shift + }q
	{i3-msg ,x}kill

# Change workspace or move container to workspace
super + {_,shift + }{1-9,0}
	i3-msg {_,move container to }workspace number {1-9,0}
# Change focus or move container
super + {_,shift + }{Left,Down,Up,Right}
	i3-msg {focus,move} {left,down,up,right}
# Resize window
super + ctrl + {Left,Down,Up,Right}
	i3-msg resize {shrink width,grow height,shrink height,grow width} 10 px or 10 ppt
# Move window to monitor or focus the next/previous desktop in the current monitor
super + alt + shift + {Left,Right}
	i3-msg move workspace to output {left,right}

alt + Return
	i3-msg workspace back_and_forth

# restart/reload/quit
super + shift + {r,i,e}
	i3-msg {restart,reload,exit}

# Toggle fullscreen
super + f
	i3-msg fullscreen toggle

super + @space
	i3-msg floating toggle

# Split direction
super + {v,h}
	i3-msg split {v,h}

# Change gap size (can inner/outer be combined to {s,z}?)
super + {_,shift + }s
	i3-msg gaps inner current {plus,minus} 3
super + {_,shift + }z
	i3-msg gaps outer current {plus,minus} 3
