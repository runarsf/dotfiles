#!/usr/bin/env bash

# SUDO_ASKPASS=~/bin/askpass-rofi

rofi -dmenu \
    -password \
    -i \
    -no-fixed-num-lines \
    -p "Password: " \
    -theme "$(eval "echo ~$USER")"/.config/rofi/themes/askpass.rasi
